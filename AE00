#include <stdio.h>


int main(){
    int N,i;
    
    scanf("%d",&N);
    
    int ans = 0;
    
    for( i = 1;i <= N / i;++i)
        ans += N / i - i + 1;
    
    printf("%d\n",ans);
    
    return 0;
}
