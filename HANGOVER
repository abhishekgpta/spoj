#include <stdio.h>

int main(void) {
	int i,count;
	float f,s=0;
	scanf("%f",&f);
	while(f!=0.00){
		i=1;count=0;s=0.00;
		while(s<=f){
			s=s+(float)1/(i+1);
			count++;
			i++;
		}
		printf("%d card(s)\n",count);
		f=0.00;
		scanf("%f",&f);
	}
	// your code goes here
	return 0;
}

